const LoginLayout = ({ children }: { children: React.ReactNode }) => (
  <div className="flex justify-center items-center w-[100%] h-[100%] px-[16px]">
    {children}
  </div>
);

export default LoginLayout;
